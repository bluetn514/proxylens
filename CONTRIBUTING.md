# 参与开发

感谢你愿意花时间改进 ProxyLens。

提交问题时，请说明 Windows 版本、使用的代理模式，以及哪个检测结果和实际情况不一致。诊断报告要先确认已经脱敏。

提交代码前请运行：

```powershell
python -m unittest discover -s tests
```

新增检测项应遵守以下原则：

- 默认只读，不修改网络配置
- 每个检测都有超时，不让界面一直等待
- 单项失败不影响其他检测
- 报告不记录公网 IP、凭据或完整代理 URL
- 提示要具体，避免把所有问题都归因于代理软件
